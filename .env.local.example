# CourtIQ — Environment Variables
# Copy this to .env.local and fill in your values

# ============================================================
# Frontend (Next.js)
# ============================================================

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# ============================================================
# Pipeline Scripts (Python — used by scripts/ and GitHub Actions)
# ============================================================
# NOTE: SUPABASE_URL is the same value as NEXT_PUBLIC_SUPABASE_URL,
# and SUPABASE_KEY is the same value as SUPABASE_SERVICE_ROLE_KEY.
# They're duplicated because Next.js and Python read env vars differently.
# In GitHub Actions, set these as repository secrets.

SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_service_role_key
