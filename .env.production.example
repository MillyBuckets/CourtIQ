# ============================================================
# CourtIQ — Production Environment Variables
# ============================================================
# Copy this file to .env.local for local development,
# or add these variables in Vercel Dashboard → Settings → Environment Variables.

# ============================================================
# Supabase (Required — Next.js frontend + API routes)
# ============================================================

# Your Supabase project URL (public)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon/public key — safe to expose in the browser (public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service role key — server-side only, NEVER exposed to the browser
# Used by API routes for unrestricted database access
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================================
# Python Data Pipeline (GitHub Actions only — NOT needed for Vercel)
# ============================================================
# Set these as GitHub repository secrets, not in Vercel.

# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your-service-role-key
